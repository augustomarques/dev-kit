interface:
  display_name: "Dev Kit: Git"
  short_description: "Deliver atomic changes through an assigned draft PR"
  default_prompt: "Use $dev-kit-git to prepare verified commits and deliver them through a draft pull request assigned to my authenticated user."
